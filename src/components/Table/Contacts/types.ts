export interface RowData {
    _id: string;
    first_name: string;
    last_name: string;
    country: string;
    organization: string;
    phone: string;
    email: string;
    message: string;
    // Add more fields as needed
  }
